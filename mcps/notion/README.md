# notion

Notion workspace.

## Installation

This MCP server is installed automatically by sentient-core. To use it manually:

```bash
npx -y notion-mcp-server
```

## Authentication

Requires an API key. Set the `NOTION_API_KEY` environment variable.

## Tools

Not yet enumerated.

## Capabilities

`notes` `wiki` `database` `knowledge_base`
