# google_calendar

Google Calendar.

## Installation

This MCP server is installed automatically by sentient-core. To use it manually:

```bash
npx -y @anthropic-ai/google-calendar-mcp
```

## Authentication

Requires OAuth2 authentication with google.

## Tools

Not yet enumerated.

## Capabilities

`schedule` `reminder` `calendar` `event`
