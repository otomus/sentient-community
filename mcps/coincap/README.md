# coincap

CoinCap crypto prices.

## Installation

This MCP server is installed automatically by sentient-core. To use it manually:

```bash
npx -y coincap-mcp
```

## Authentication

No authentication required.

## Tools

- get_price
- list_assets

## Capabilities

`crypto` `bitcoin` `price` `finance` `coin`
