# github

GitHub integration.

## Installation

This MCP server is installed automatically by sentient-core. To use it manually:

```bash
npx -y @anthropic-ai/github-mcp
```

## Authentication

Requires an API key. Set the `GITHUB_TOKEN` environment variable.

## Tools

Not yet enumerated.

## Capabilities

`git` `code` `issue` `pull_request`
