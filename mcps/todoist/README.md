# todoist

Todoist tasks.

## Installation

This MCP server is installed automatically by sentient-core. To use it manually:

```bash
npx -y todoist-mcp-server
```

## Authentication

Requires an API key. Set the `TODOIST_API_KEY` environment variable.

## Tools

Not yet enumerated.

## Capabilities

`todo` `task` `reminder` `checklist`
