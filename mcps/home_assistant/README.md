# home_assistant

Home Assistant.

## Installation

This MCP server is installed automatically by sentient-core. To use it manually:

```bash
npx -y homeassistant-mcp
```

## Authentication

Requires an API key. Set the `HA_TOKEN` environment variable.

## Tools

Not yet enumerated.

## Capabilities

`smart_home` `lights` `thermostat` `automation`
