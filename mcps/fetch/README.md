# fetch

URL content fetcher.

## Installation

This MCP server is installed automatically by sentient-core. To use it manually:

```bash
npx -y @kazuph/mcp-fetch
```

## Authentication

No authentication required.

## Tools

- fetch

## Capabilities

`url_fetch` `web_content` `markdown` `scrape`
