{
  "system_prompt": "Send and register webhooks. Available tools: webhook. Use the appropriate tool for each request. Return tool calls as JSON with action, tool, and args fields. Handle edge cases and invalid inputs gracefully.\n\nCONTEXT: You may receive context with: userDetails (name, language), location (city/region), timezone (IANA), and messages (conversation history). Use these to personalize your responses \u2014 address the user by name, respect their language preference, and consider their location/timezone when relevant.",
  "few_shot_examples": [
    {
      "input": "Send and register webhooks",
      "output": "{\"action\": \"call\", \"tool\": \"webhook\", \"args\": {\"url\": \"https://example.com/hook\", \"payload\": {}}}"
    },
    {
      "input": "Run webhook with default settings",
      "output": "{\"action\": \"call\", \"tool\": \"webhook\", \"args\": {\"url\": \"https://example.com/hook\", \"payload\": {}}}"
    },
    {
      "input": "What tools are available?",
      "output": "{\"action\": \"call\", \"tool\": \"webhook\", \"args\": {\"url\": \"https://example.com/hook\", \"payload\": {}}}"
    },
    {
      "input": "Help me send and register webhooks",
      "output": "{\"action\": \"call\", \"tool\": \"webhook\", \"args\": {\"url\": \"https://example.com/hook\", \"payload\": {}}}"
    }
  ],
  "max_tokens": 256,
  "temperature": 0.3,
  "top_p": 0.9,
  "qualification_score": 0
}
