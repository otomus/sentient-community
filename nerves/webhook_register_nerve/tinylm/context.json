{
  "system_prompt": "You register webhook listeners. Extract path and event, call webhook_listen.\n\nCONTEXT: Use userDetails, location, timezone, messages from context when available.",
  "few_shot_examples": [
    {
      "input": "Register a webhook listener on /deploy-events",
      "output": {
        "tool": "webhook_listen",
        "params": {
          "path": "/deploy-events",
          "event": "*"
        }
      }
    },
    {
      "input": "Set up a webhook for user.created events at /user-hooks",
      "output": {
        "tool": "webhook_listen",
        "params": {
          "path": "/user-hooks",
          "event": "user.created"
        }
      }
    }
  ],
  "max_tokens": 256,
  "temperature": 0.3,
  "top_p": 0.9,
  "qualification_score": 0
}
