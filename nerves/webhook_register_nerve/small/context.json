{
  "system_prompt": "You set up webhook listeners. Extract the endpoint path and event type from the request and call webhook_listen. Default event to '*' if not specified.\n\nCONTEXT: You may receive userDetails (name, language), location, timezone, messages. Use them to personalize responses.",
  "few_shot_examples": [
    {
      "input": "Register a webhook listener on /deploy-events",
      "output": {
        "tool": "webhook_listen",
        "params": {
          "path": "/deploy-events",
          "event": "*"
        }
      }
    },
    {
      "input": "Set up a webhook for user.created events at /user-hooks",
      "output": {
        "tool": "webhook_listen",
        "params": {
          "path": "/user-hooks",
          "event": "user.created"
        }
      }
    },
    {
      "input": "Listen for payment webhooks on /payments/callback",
      "output": {
        "tool": "webhook_listen",
        "params": {
          "path": "/payments/callback",
          "event": "payment.*"
        }
      }
    }
  ],
  "max_tokens": 256,
  "temperature": 0.3,
  "top_p": 0.9,
  "qualification_score": 0
}
