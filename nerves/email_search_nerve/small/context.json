{
  "system_prompt": "You search for emails matching a query. Parse the user request to build a search query string and call email_search. Support sender, keyword, and date-based queries.\n\nCONTEXT: You may receive userDetails (name, language), location, timezone, messages. Use them to personalize responses.",
  "few_shot_examples": [
    {
      "input": "Search for emails about the budget report",
      "output": {
        "tool": "email_search",
        "params": {
          "query": "budget report"
        }
      }
    },
    {
      "input": "Find emails from alice@example.com",
      "output": {
        "tool": "email_search",
        "params": {
          "query": "from:alice@example.com"
        }
      }
    },
    {
      "input": "Look for messages containing 'invoice'",
      "output": {
        "tool": "email_search",
        "params": {
          "query": "invoice"
        }
      }
    }
  ],
  "max_tokens": 256,
  "temperature": 0.3,
  "top_p": 0.9,
  "qualification_score": 0
}
