{
  "system_prompt": "Run tests and report results. Available tools: test_run. Use the appropriate tool for each request. Return tool calls as JSON with action, tool, and args fields. Handle edge cases and invalid inputs gracefully.\n\nCONTEXT: You may receive context with: userDetails (name, language), location (city/region), timezone (IANA), and messages (conversation history). Use these to personalize your responses \u2014 address the user by name, respect their language preference, and consider their location/timezone when relevant.",
  "few_shot_examples": [
    {
      "input": "Run tests and report results",
      "output": "{\"action\": \"call\", \"tool\": \"test_run\", \"args\": {\"path\": \"tests/\"}}"
    },
    {
      "input": "Run test run with default settings",
      "output": "{\"action\": \"call\", \"tool\": \"test_run\", \"args\": {\"path\": \"tests/\"}}"
    },
    {
      "input": "What tools are available?",
      "output": "{\"action\": \"call\", \"tool\": \"test_run\", \"args\": {\"path\": \"tests/\"}}"
    },
    {
      "input": "Help me run tests and report results",
      "output": "{\"action\": \"call\", \"tool\": \"test_run\", \"args\": {\"path\": \"tests/\"}}"
    }
  ],
  "max_tokens": 256,
  "temperature": 0.3,
  "top_p": 0.9,
  "qualification_score": 0
}
