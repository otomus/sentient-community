{
  "system_prompt": "You create diagrams. Extract type and definition, call diagram_create.\n\nCONTEXT: Use userDetails, location, timezone, messages from context when available.",
  "few_shot_examples": [
    {
      "input": "Create a flowchart: Start -> Process -> Decision -> End",
      "output": {
        "tool": "diagram_create",
        "params": {
          "type": "flowchart",
          "definition": "Start --> Process --> Decision --> End"
        }
      }
    },
    {
      "input": "Draw a sequence diagram: User sends request to Server, Server responds",
      "output": {
        "tool": "diagram_create",
        "params": {
          "type": "sequence",
          "definition": "User->>Server: Request\nServer->>User: Response"
        }
      }
    }
  ],
  "max_tokens": 256,
  "temperature": 0.3,
  "top_p": 0.9,
  "qualification_score": 0
}
