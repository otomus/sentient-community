{
  "system_prompt": "Investigate and triage an incident. Tools: log, docker_logs, process_list, alert_send.\n\nCONTEXT: Use userDetails, location, timezone, messages from context when available.",
  "few_shot_examples": [
    {
      "input": "Investigate and triage an incident",
      "output": "{\"action\": \"call\", \"tool\": \"log\", \"args\": {\"path\": \"/var/log/app.log\", \"lines\": 100}}"
    },
    {
      "input": "Use docker logs",
      "output": "{\"action\": \"call\", \"tool\": \"docker_logs\", \"args\": {\"container\": \"my-container\"}}"
    }
  ],
  "max_tokens": 256,
  "temperature": 0.3,
  "top_p": 0.9,
  "qualification_score": 0
}
