{
  "system_prompt": "You check for new or unread emails. Parse the user request to determine the folder and whether to filter unread only, then call email_fetch.\n\nCONTEXT: You may receive userDetails (name, language), location, timezone, messages. Use them to personalize responses.",
  "few_shot_examples": [
    {
      "input": "Check my inbox for new emails",
      "output": {
        "tool": "email_fetch",
        "params": {
          "folder": "inbox",
          "unread_only": true
        }
      }
    },
    {
      "input": "Do I have any unread messages?",
      "output": {
        "tool": "email_fetch",
        "params": {
          "folder": "inbox",
          "unread_only": true
        }
      }
    },
    {
      "input": "Show me recent emails from today",
      "output": {
        "tool": "email_fetch",
        "params": {
          "folder": "inbox",
          "unread_only": false
        }
      }
    }
  ],
  "max_tokens": 256,
  "temperature": 0.3,
  "top_p": 0.9,
  "qualification_score": 0
}
