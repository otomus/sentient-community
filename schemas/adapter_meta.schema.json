{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Brain Adapter Meta",
  "description": "Model information and contributor details for a brain adapter",
  "type": "object",
  "required": ["model", "size_class", "contributor"],
  "properties": {
    "model": {
      "type": "string",
      "description": "Model identifier (e.g., llama3.2-3b)"
    },
    "size_class": {
      "type": "string",
      "enum": ["tinylm", "small", "medium", "large"]
    },
    "provider": {
      "type": "string",
      "enum": ["ollama", "anthropic", "groq", "openai_compat"]
    },
    "contributor": {
      "type": "object",
      "required": ["github"],
      "properties": {
        "github": { "type": "string" },
        "name": { "type": "string" }
      }
    },
    "verified": {
      "type": "boolean",
      "default": false
    },
    "capabilities": {
      "type": "object",
      "properties": {
        "json_mode": { "type": "boolean" },
        "tool_calling": { "type": "boolean" },
        "max_context": { "type": "integer", "minimum": 1 },
        "max_messages": {
          "type": "integer",
          "minimum": 1,
          "description": "Sliding window size — max conversation messages to include in context"
        },
        "include_summary": {
          "type": "boolean",
          "default": false,
          "description": "Whether to prepend a summary of earlier conversation when window is exceeded"
        }
      }
    },
    "tuning": {
      "type": "object",
      "description": "Inference tuning parameters for this size class",
      "properties": {
        "min_training_examples": {
          "type": "integer",
          "minimum": 1,
          "description": "Minimum I/O pairs before inference tuning kicks in"
        },
        "test_cases_per_batch": {
          "type": "integer",
          "minimum": 1,
          "description": "Number of test cases the critic generates per evaluation round"
        },
        "few_shot_limit": {
          "type": "integer",
          "minimum": 0,
          "description": "Max few-shot examples included in the prompt"
        },
        "temperature_range": {
          "type": "array",
          "items": { "type": "number", "minimum": 0.0, "maximum": 2.0 },
          "minItems": 1,
          "description": "Candidate temperatures the system will trial when optimizing for this role/model"
        }
      }
    },
    "qualification": {
      "type": "object",
      "description": "Pass/fail thresholds and iteration limits for adapter qualification",
      "properties": {
        "low_quality_threshold": {
          "type": "number",
          "minimum": 0.0,
          "maximum": 1.0,
          "description": "Floor score — submissions below this are auto-rejected"
        },
        "minimum_threshold": {
          "type": "number",
          "minimum": 0.0,
          "maximum": 1.0,
          "description": "Minimum score to pass qualification — adapter is usable above this"
        },
        "golden_threshold": {
          "type": "number",
          "minimum": 0.0,
          "maximum": 1.0,
          "description": "Golden standard score — adapter is fully production-ready above this"
        },
        "min_iterations": {
          "type": "integer",
          "minimum": 1,
          "description": "Minimum qualification attempts before accepting minimum_threshold"
        },
        "golden_iterations": {
          "type": "integer",
          "minimum": 1,
          "description": "Maximum qualification attempts to reach golden_threshold"
        },
        "qualification_timeout": {
          "type": "integer",
          "minimum": 1,
          "description": "Total timeout in seconds for the qualification loop"
        },
        "low_quality_threshold": {
          "type": "number",
          "minimum": 0.0,
          "maximum": 1.0,
          "description": "Nerves scoring below this after qualification are pruned"
        },
        "merge_threshold": {
          "type": "number",
          "minimum": 0.0,
          "description": "Minimum similarity score to consider two nerves as merge candidates"
        },
        "improvement_threshold": {
          "type": "number",
          "minimum": 0.0,
          "maximum": 1.0,
          "description": "Nerves above this score are considered fully tuned and skip reconciliation"
        }
      }
    }
  }
}
