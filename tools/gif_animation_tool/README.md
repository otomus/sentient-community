# gif_animation_tool

Generate animated GIFs from text descriptions using an external animation service.

## Usage

```python
from tool import run

url = run("a cat dancing on a rainbow")
print(url)
```

## Parameters

- `query` (string, required): Text description of the animation to generate.
