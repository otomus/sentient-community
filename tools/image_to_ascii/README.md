# image_to_ascii

Convert images to ASCII art by providing an image URL.

## Usage

```python
from tool import run

ascii_art = run("https://example.com/photo.png")
print(ascii_art)
```

## Parameters

- `query` (string, required): URL of the image to convert.
