# video_editing_tool

Generate animated GIFs from text descriptions using external video editing services.

## Usage

```python
from tool import run

url = run("fireworks display over a city skyline")
print(url)
```

## Parameters

- `query` (string, required): Text description of the video or animation to generate.
